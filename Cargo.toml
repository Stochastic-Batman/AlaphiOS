[workspace]
members = ["kernel"]
exclude = ["tools/image-builder"]
resolver = "2"

# no_std kernels cannot unwind; abort on panic in both profiles.
# image-builder lives outside this workspace and is unaffected.
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
